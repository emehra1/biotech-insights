node_modules
.next
out
.DS_Store
.env
.env.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# build artifacts
*.tsbuildinfo

# derived at build time from data/ — never committed
public/data/

# raw fetched bodies (large, copyrighted); http-meta.json IS committed
.cache/raw/

# local email previews
.preview/

# legacy artifact of the old generate-summary script
summary.json
